# Setup
- `npm install --save-dev gh-pages` to install github pages
- `npm i` && `npm run dev` to test locally
- `npm run build` && `npm run deploy` to deploy
- URL of app: `https://nguyenlevo.github.io/popcorn.github.io/`